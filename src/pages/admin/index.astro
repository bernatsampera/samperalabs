---
import Button from '~/components/ui/Button.astro';
import Layout from '../../layouts/Layout.astro';

const metadata = {
  title: 'Admin',
};
---

<Layout metadata={metadata}>
  <div class="container mx-auto px-6 py-16 max-w-md">
    <div id="login-form" class="bg-ink-2 border border-ink-border rounded-lg p-6">
      <h1 class="text-2xl font-bold mb-6 text-center text-text-strong">Admin Login</h1>
      <form id="admin-login-form" class="space-y-4">
        <div>
          <label for="password" class="block text-sm font-medium mb-2 text-text-strong">API key</label>
          <input
            type="password"
            id="password"
            name="password"
            class="w-full px-3 py-2 border border-ink-border rounded-md bg-ink-2 text-text-strong placeholder:text-text-faint"
            autocomplete="off"
            required
          />
        </div>
        <button
          type="submit"
          class="w-full px-4 py-2 bg-stamp text-ink rounded-md hover:bg-stamp/90 transition-colors"
        >
          Login
        </button>
      </form>
      <div id="error-message" class="mt-4 text-red-400 text-sm hidden"></div>
    </div>

    <div id="admin-panel" class="bg-ink-2 border border-ink-border rounded-lg p-6 hidden">
      <h1 class="text-2xl font-bold mb-6 text-center text-text-strong">Admin Panel</h1>
      <p class="text-center text-text-muted mb-6">You are now logged in as admin.</p>
      <div class="space-y-4">
        <a
          href="/blog"
          class="block w-full px-4 py-2 bg-stamp text-ink rounded-md hover:bg-stamp/90 transition-colors text-center"
        >
          Go to Blog
        </a>
        <Button href="/admin/new-post" text="Create New Post" class="w-full" />
        <button
          id="logout-btn"
          class="w-full px-4 py-2 bg-red-600 text-white rounded-md hover:bg-red-600/90 transition-colors"
        >
          Logout
        </button>
      </div>
    </div>
  </div>
</Layout>

<script>
  // Check if user is already logged in
  const isAdmin = localStorage.getItem('isAdmin');
  const loginForm = document.getElementById('login-form');
  const adminPanel = document.getElementById('admin-panel');

  if (isAdmin === 'true') {
    loginForm?.classList.add('hidden');
    adminPanel?.classList.remove('hidden');
  }

  // Handle login form submission — validate the pasted key against the API
  document.getElementById('admin-login-form')?.addEventListener('submit', async (e) => {
    e.preventDefault();

    const formData = new FormData(e.target as HTMLFormElement);
    const key = (formData.get('password') as string).trim();
    const errorMessage = document.getElementById('error-message');

    if (!key) {
      errorMessage!.textContent = 'API key is required.';
      errorMessage?.classList.remove('hidden');
      return;
    }

    try {
      const probe = await fetch('/api/posts?limit=1', {
        headers: { Authorization: `Bearer ${key}` },
      });
      if (probe.ok) {
        localStorage.setItem('BLOG_API_KEY', key);
        localStorage.setItem('isAdmin', 'true');
        loginForm?.classList.add('hidden');
        adminPanel?.classList.remove('hidden');
        errorMessage?.classList.add('hidden');
      } else if (probe.status === 401) {
        errorMessage!.textContent = 'Invalid API key.';
        errorMessage?.classList.remove('hidden');
      } else {
        errorMessage!.textContent = `Login failed (${probe.status}).`;
        errorMessage?.classList.remove('hidden');
      }
    } catch (err) {
      console.error('Login check failed:', err);
      errorMessage!.textContent = 'Network error. Try again.';
      errorMessage?.classList.remove('hidden');
    }
  });

  // Handle logout
  document.getElementById('logout-btn')?.addEventListener('click', () => {
    localStorage.removeItem('isAdmin');
    localStorage.removeItem('BLOG_API_KEY');
    adminPanel?.classList.add('hidden');
    loginForm?.classList.remove('hidden');

    const passwordField = document.getElementById('password') as HTMLInputElement;
    if (passwordField) passwordField.value = '';

    document.getElementById('error-message')?.classList.add('hidden');
  });
</script>
